extern int compare(void const *, void const *);

long long *handle = scc_rbtree_init(long long, compare);
for(int i = 38; i >= 0; i -= 2) {
    scc_rbtree_insert(handle, i + 0ll);
}

long long const *iter;
scc_rbtree_foreach(iter, handle) {
    printf("%lld ", *iter);
}
puts("");

scc_rbtree_free(handle);
