int *vec = scc_vec_init(int);

/* Insert an arbitrary number of elements */
scc_vec_push(vec, 1);
scc_vec_push(vec, 2);
scc_vec_push(vec, 3);

int *iter;
puts("Vector contents:");
scc_vec_foreach(iter, vec) {
    printf("%d\n", *iter);
}

scc_vec_free(vec);
