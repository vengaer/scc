#include <scc/scc_vec.h>

/* Uses scc_svec */
scc_stack(int) svec_stack = scc_stack_init(int);

/* ... */

#undef SCC_STACK_CONTAINER
#define SCC_STACK_CONTAINER scc_vec
scc_stack(int) vec_stack = scc_stack_init(int);

/* Bug, operating on an scc_svec as if it were
 * an scc_vec */
scc_stack_push(svec_stack, 38);
