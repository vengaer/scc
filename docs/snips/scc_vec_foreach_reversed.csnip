double *vec = scc_vec_init(int);
double *iter;

/* Fill up the vector */
scc_vec_push(vec, 2.3);
scc_vec_push(vec, 12.8);
scc_vec_push(vec, 0.0);

scc_vec_foreach_reversed(iter, vec) {
    printf("%f\n", *iter);
}

scc_vec_free(vec);
